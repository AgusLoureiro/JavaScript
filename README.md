# JavaScript
Simulador interactivo

if (
  isNaN(cantCuotas) ||
  cantCuotas == null ||
  cantCuotas == 0 ||
  cantCuotas == "" ||
  cantCuotas > 12
) {
  // alert("Ingrese una cantidad de cuotas válida")
} else {
}